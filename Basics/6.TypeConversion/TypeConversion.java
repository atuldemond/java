import java.utils.*;
public class TypeConversion{
    public static void main (String args[]){
        //1. type conversion means if byte short char => int
        //=> (a+ b+ c) its treated as int 

        //2. type conversion if long float double then => double
        //=> (a+b+c ) its treated as double 

        //** if any expression came into role then type conversion is Gone work forstly */
    }
}